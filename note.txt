
// the routed will take two optional params the state that can be served as cahcing layer that will
// survive
// dont know if we can inject the types automatically in dev mode so that user dont get to do or
// maybe generate the types of the params segment and make the user import
/

export function routed(params, state) {
 // if this is mark as an async the return value aside from the original data
 // say return {data} the user will get another thing $status which will be pending,completed,error
 // this can help us suspend this component if the user want in the component
}


function Hello() {

}
