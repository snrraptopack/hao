{
  "name": "auwla-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/server.ts --outDir dist/server",
    "serve": "bun run dist/server/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "auwla": "link:auwla"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.16",
    "bun-types": "latest"
  }
}
