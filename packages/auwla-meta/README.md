# @auwla/meta

Meta/runtime and plugins for Auwla, structured as a separate workspace package.

Currently sources are in the root `meta` folder and imported via TypeScript path aliases. Once stabilized, code can be moved under `packages/auwla-meta/src`.